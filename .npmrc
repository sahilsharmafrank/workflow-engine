# This project is a standalone product intended to be installable by anyone.
# Pin the public npm registry so a machine-level private-registry config does
# not make the build depend on credentials external users cannot have.
registry=https://registry.npmjs.org/
